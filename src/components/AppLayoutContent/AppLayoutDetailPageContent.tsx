import React from 'react';
import { AssetType } from '../../helpers/types';

const AppLayoutDetailPageContent: React.FC<AssetType> = (asset: any) => {
    
    return (
        <div>
            
        </div>
    )
};

export default AppLayoutDetailPageContent;