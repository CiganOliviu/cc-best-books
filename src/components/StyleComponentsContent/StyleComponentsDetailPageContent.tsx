import React from 'react';
import { AssetType } from '../../helpers/types';

const StyleComponentsDetailPageContent: React.FC<AssetType> = (asset: any) => {
    
    return (
        <div>
            
        </div>
    )
};

export default StyleComponentsDetailPageContent;